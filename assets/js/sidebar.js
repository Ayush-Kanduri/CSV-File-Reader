{
	try {
		// const sidebar = document.querySelector("#sidebar");
		// const toggler = document.querySelector("#toggle-sidebar");
		// const icon = toggler.querySelector("i");
		// function sidebarToggle() {
		// const close = sidebar.getAttribute("data-close");
		// const after = window.getComputedStyle(sidebar, "::after");
		// const content = after["content"];
		// const content = after.getPropertyValue("content");
		// sidebar.style.setProperty("--url", `url(${close})`);
		// }
		// sidebarToggle();
	} catch (error) {
		console.log(error);
	}
}
